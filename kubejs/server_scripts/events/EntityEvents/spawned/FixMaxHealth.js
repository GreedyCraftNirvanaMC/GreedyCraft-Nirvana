// 实体事件-生成事件
// 此脚本用于限制生成生物的最大血量（暂时不确定是否需要此脚本）
// priority: 50

// EntityEvents.spawned(event => {
//     if (event.entity.isLiving() && event.entity.getMaxHealth() > 1048576.0) {
//         event.cancel()
//     }
// })
